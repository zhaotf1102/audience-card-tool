# 项目需求说明：观众信息卡裁剪与 PPT 自动生成工具

## 1. 业务背景

观众入场前在门票副券上勾选信息，并填写自我评价。男生使用蓝色卡片，女生使用粉色卡片。

工作人员收集副券后，会按照性别与特质分类放入总卡：

- 男生：高 / 富 / 帅
- 女生：白 / 富 / 美

每个特质有 2 张总卡，用于覆盖更多观众数量，因此一场活动通常最多 12 张总卡。

## 2. 总卡规则

- 总卡固定版式一致。
- 每张总卡为横 9 列 × 竖 3 行。
- 每张总卡共 27 个卡槽。
- 蓝色总卡代表男生。
- 粉色总卡代表女生。

## 3. 裁剪规则

每张大总卡裁剪为 2 张小总卡：

1. 左侧小总卡：第 1-4 列 × 第 1-3 行，共 12 个卡槽。
2. 右侧小总卡：第 5-9 列 × 第 1-3 行，共 15 个卡槽。

注意：

- 不能裁漏信息。
- 不能让同一张副券同时出现在两张小总卡中。
- 不能基于观众文字内容去重。
- 裁剪前后卡槽数量要保持一致。

## 4. 分类规则

第一版以人工确认为主：

- 上传后，用户为每张照片选择分类。
- 可根据文件名辅助识别分类。
- 可根据卡片颜色与标题做自动建议，但不能强制替代人工确认。

分类枚举：

- male_tall：男高
- male_rich：男富
- male_handsome：男帅
- female_fair：女白
- female_rich：女富
- female_beautiful：女美

## 5. 复查功能规划

MVP 暂不强制做副券勾选识别。正式版可以加入疑似错放提醒：

- 系统扫描每个副券勾选区域。
- 如果副券疑似勾选项与所在总卡分类不一致，则标红提示。
- 系统只提示，不自动移动或删除。

## 6. PPT 生成规则

用户提前上传一个 PPT 模板。

工具需要：

1. 读取 PPT 模板。
2. 找到 6 类主题页。
3. 把对应分类的小总卡图片插入对应主题页。
4. 每页只放一张小总卡。
5. 如果某个分类的小总卡数量超过模板已有页数，则复制该分类模板页。
6. 图片在页面中居中或放入预设安全区域，保证清晰可读。
7. 导出新的 PPT 文件。

## 7. PPT 分类映射建议

因为男富和女富标题都可能出现“富”，不能只靠单字标题判断。

MVP 建议使用固定页码配置：

```json
{
  "male_tall": [1],
  "male_rich": [2],
  "male_handsome": [3],
  "female_fair": [4],
  "female_rich": [5],
  "female_beautiful": [6]
}
```

正式版可支持：

- 页面备注隐藏标记，例如 `{{male_tall}}`。
- 页面透明文本占位符，例如 `AUTO_SLOT_male_tall`。
- 后台配置页码。

## 8. MVP 范围

MVP 必做：

- 本地网页上传。
- 批量照片管理。
- 人工分类确认。
- 固定 9×3 裁剪。
- 左 4×3 / 右 5×3 输出。
- 裁剪预览。
- PPT 模板上传。
- 按分类生成 PPT。
- 导出 PPT。

MVP 不做：

- 用户登录。
- 复杂数据库。
- 小程序。
- App。
- 手写 OCR。
- 自动删除重复。
- 复杂美化。

## 9. 建议现场拍摄标准

- 每张总卡单独拍。
- 四个角完整入镜。
- 尽量正对拍摄。
- 避免闪光灯和强反光。
- 上传原图，不要经过微信压缩。
- 文件名建议带分类，例如：`男-高-01.jpg`。