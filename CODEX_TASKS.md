# Codex 开发任务清单

## 今晚目标

先完成一个能跑通主流程的本地 MVP：

> 上传照片 → 人工确认分类 → 自动裁剪左 4×3 / 右 5×3 → 上传 PPT 模板 → 按分类生成新 PPT。

不要一开始做复杂自动识别、账号系统、数据库、小程序或 App。

## 优先级 P0：项目跑起来

- [ ] 后端 FastAPI 可以启动。
- [ ] 前端 Vite/React 可以启动。
- [ ] 前后端能联通。
- [ ] 建立本地 workspace 文件夹，保存上传文件与输出文件。

验收标准：

- 打开前端页面不报错。
- 后端 `/api/health` 返回 OK。

## 优先级 P1：图片上传与分类确认

- [ ] 支持批量上传图片。
- [ ] 每张图片生成一个 card record。
- [ ] 前端显示图片列表。
- [ ] 每张图片可以人工选择分类：男高、男富、男帅、女白、女富、女美。
- [ ] 支持从文件名粗略推断分类，但用户可以修改。

验收标准：

- 上传 12 张总卡后，前端能逐张显示。
- 用户可以为每张照片选择分类并保存。

## 优先级 P2：固定版式裁剪

- [ ] 后端读取图片。
- [ ] 按大总卡整体区域处理。
- [ ] 第一版可以先把整张照片当作总卡区域，后续再加自动边界识别。
- [ ] 按 9×3 网格逻辑裁剪。
- [ ] 输出左图：第 1-4 列 × 第 1-3 行。
- [ ] 输出右图：第 5-9 列 × 第 1-3 行。
- [ ] 保存裁剪图片。
- [ ] 前端显示裁剪预览。

验收标准：

- 每张大总卡输出 2 张小总卡。
- 左图和右图卡槽不重复。
- 不基于文字相似度删除任何图片。

## 优先级 P3：PPT 生成

- [ ] 支持上传 PPT 模板。
- [ ] 第一版使用固定页码配置识别 6 个分类模板页。
- [ ] 每页插入 1 张小总卡图片。
- [ ] 如果某分类图片数量超过模板页数量，则复制该分类模板页。
- [ ] 生成新的 PPTX。
- [ ] 前端提供下载按钮。

验收标准：

- 选择图片分类后，可以生成新 PPT。
- 每张小总卡占 1 页。
- 图片大小合理，居中，清晰。

## 优先级 P4：稳定性增强

- [ ] 增加边距配置。
- [ ] 增加裁剪线人工微调。
- [ ] 增加自动识别蓝/粉性别。
- [ ] 增加总卡外框检测和透视校正。
- [ ] 增加模糊/反光提醒。
- [ ] 导出 Excel 复查表。

## 技术建议

后端：

- FastAPI
- OpenCV
- Pillow
- python-pptx
- pydantic
- python-multipart

前端：

- React
- Vite
- 原生 CSS 即可，不要为了 MVP 上复杂 UI 库

## 注意事项

- 不要提交真实观众照片。
- 不要把 outputs、uploads、workspace 提交到 GitHub。
- 不要因为两张副券文字相似就删除。
- 不要只看“富”字区分男富和女富，必须使用分类枚举。